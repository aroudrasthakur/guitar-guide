# Copy to `.env.local` for local development (Next.js reads `.env.local`).
#
# Currently, this app does not require any environment variables.
# Add variables here as you introduce them, e.g.:
#
# NEXT_PUBLIC_SOME_FLAG="true"
